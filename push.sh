git add .
git commit -m"crearted the export data custom management command to export the data from any model to any csv file with the corresponding name of the model and the time "
git branch -M main
git push origin main
